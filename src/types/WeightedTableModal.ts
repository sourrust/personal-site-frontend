import ModalType from './WeightedTableModalType';

interface WeightedTableModal {
    isActive: boolean;
    name: string;
    weight: string;
    type: ModalType;
}

export default WeightedTableModal;
